{
  "name": "nextsite-app",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/nextsite-app",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "root": "apps/nextsite-app",
        "outputPath": "dist/apps/nextsite-app"
      },
      "configurations": {
        "development": {
          "outputPath": "apps/nextsite-app"
        },
        "production": {}
      }
    },
    "serve": {
      "executor": "@nrwl/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "nextsite-app:build",
        "dev": true
      },
      "configurations": {
        "development": {
          "buildTarget": "nextsite-app:build:development",
          "dev": true
        },
        "production": {
          "buildTarget": "nextsite-app:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nrwl/next:export",
      "options": {
        "buildTarget": "nextsite-app:build:production"
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/nextsite-app/jest.config.ts",
        "passWithNoTests": true
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/nextsite-app/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
