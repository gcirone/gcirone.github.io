{
  "name": "gc-image-generator",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "wrangler dev",
    "build": "echo build",
    "dev": "wrangler dev",
    "dev:remote": "wrangler dev --remote",
    "deploy": "wrangler deploy",
    "test": "vitest",
    "typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.2",
    "@cloudflare/workers-types": "^4.20241011.0",
    "lodash-es": "^4.17.21",
    "typescript": "^5.6.3",
    "vitest": "2.0.5",
    "wrangler": "^3.80.5"
  }
}
